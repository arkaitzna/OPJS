//Notacion

//; --> Delimita el final de una linea de codigo.

const b = 4;
console.log(b + 4);

//. --> Se utiliza  en los objetos para acceder a los atributos(altura, anchura, profundidad...)

// movil.anchura


//{} --> Llaves para objetos, funciones y estructuras de control
const movil={
    anchura: 5,
    altura:10
}

if (true){
    //todo lo que haya entre llaves

    const constante2='hola'
}
else{
    const3='adios'
}


//[] --> Se utilizan para las listas, arreglos o arrays

const ar = [1,2,3,4];
console.log(ar[2])

//()--> Funciones

function suma(a,b){
    //Se escribe la funcion
}
